pre-commit:
  parallel: true
  commands:
    linter:
      glob: '*.{js}'
      run: pnpm eslint {staged_files} --fix
    prettier:
      glob: '*.{js,json,yml,yaml,md}'
      run: pnpm prettier --write {staged_files}
